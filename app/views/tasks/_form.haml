= simple_form_for [project, task], html: { class: 'form-horizontal' } do |f|
  = f.input :title
  = f.input :estimate, as: :date
  = f.input :tag_list, as: :text, label: "Tags (separated by commas)"
  .form-group
    %p Perfomer
    = f.select :performer_id, options_from_collection_for_select(project.users, "id", "email"), {},class: "form-control"


  = f.button :submit